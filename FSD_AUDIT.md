# Аудит соответствия FSD архитектуре

## 🔍 Проверка структуры

### ✅ Корректная структура FSD

```
src/
├── app/              ✅ Инициализация приложения
├── pages/            ✅ Страницы
├── widgets/          ✅ Виджеты
├── features/         ✅ Features
├── entities/          ✅ Entities
└── shared/            ✅ Shared
```

## ⚠️ Найденные проблемы

### 1. Старые директории (legacy)
- `src/components/` - содержит компоненты, которые должны быть в FSD слоях
- `src/hooks/` - содержит хуки, которые должны быть в entities
- `src/utils/` - содержит утилиты, которые должны быть в shared
- `src/config/` - содержит конфигурации, которые должны быть в shared
- `src/constants/` - содержит константы, которые должны быть в shared
- `src/types/` - содержит типы, которые должны быть в entities
- `src/api/` - содержит API, которые должны быть в entities

### 2. Дублирование компонентов
- `src/components/ErrorBoundary.tsx` - должен быть в `shared/ui/`
- `src/components/LazyErrorBoundary.tsx` - должен быть в `shared/ui/`
- `src/components/LoadingFallback.tsx` - должен быть в `shared/ui/`

### 3. Использование старых импортов
- `src/App.tsx` - использует `useApiData` из `@/hooks/useApiData` вместо entities
- Некоторые файлы все еще используют старые пути

## 📋 Рекомендации

### Приоритет 1: Критичные
1. Перенести Error Boundary компоненты в `shared/ui/`
2. Обновить `useApiData` - использовать entities вместо hooks
3. Удалить дублирование компонентов

### Приоритет 2: Важные
1. Постепенно перенести компоненты из `components/` в соответствующие FSD слои
2. Обновить все импорты на использование FSD структуры

### Приоритет 3: Опциональные
1. Удалить старые директории после полной миграции
2. Обновить документацию

