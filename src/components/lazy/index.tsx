/**
 * =============================================================================
 * LAZY LOADING КОМПОНЕНТОВ
 * Динамическая загрузка компонентов для code splitting
 * =============================================================================
 * 
 * Использование React.lazy для разбиения бандла на чанки.
 * Каждый шаг формы загружается только когда нужен.
 * 
 * @module lazy
 */

import { lazy } from 'react'

/**
 * Шаг 1: Детали услуги подключения
 * Загружается при выборе услуги
 */
export const FirstStepOfForm = lazy(() => import('../FirstStepOfForm'))

/**
 * Шаг 2: Основание обращения
 * Загружается после выбора услуги
 */
export const TwoStepOfAccordion = lazy(
    () => import('../TwoStepOfAccordion'),
)

/**
 * Шаг 3: Сведения о заявителе
 * Загружается после выбора основания обращения
 * Примечание: Используется ClientInfoStep напрямую (не lazy), так как он уже оптимизирован
 */

/**
 * Шаг 4: Информация об объекте
 * Загружается после начала заполнения данных заявителя
 */
export const ForeStepOfInfoObj = lazy(
    () => import('../ForeStepOfInfoObj'),
)

/**
 * Шаг 5: Загрузка документов
 * Загружается перед отправкой формы
 */
export const DocumentsUploadForm = lazy(
    () => import('../DocumentsUploadForm'),
)

/**
 * Страница успешной отправки
 * Загружается только после успешной отправки
 */
export const SuccessPage = lazy(() => import('../SuccessPage'))

