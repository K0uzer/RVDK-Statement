/**
 * Типы для сущности Client (Заявитель)
 * 
 * FSD: entities/client/model - типы и модели заявителя
 */

import type {
    IndividualClient,
    LegalClient,
    LegalClientIP,
    LegalClientGov,
    ClientType,
    FullName,
    Trustee,
} from '../../request/model/types'

export type {
    IndividualClient,
    LegalClient,
    LegalClientIP,
    LegalClientGov,
    ClientType,
    FullName,
    Trustee,
}
