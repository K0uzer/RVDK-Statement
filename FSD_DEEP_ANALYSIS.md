# Глубокий анализ соответствия FSD архитектуре

## 🔴 Критические нарушения

### 1. Директория `src/components/` - НЕ соответствует FSD

**Проблема**: Большая директория с компонентами, которые должны быть в разных FSD слоях.

#### 1.1. `src/components/ui/` (60+ файлов)
**Текущее состояние**: Все UI компоненты находятся в `src/components/ui/`
**Должно быть**: `src/shared/ui/`
**Причина**: UI компоненты - это переиспользуемые компоненты, должны быть в `shared/ui`

#### 1.2. `src/components/forms/`
**Текущее состояние**: Формы заявителей в `src/components/forms/`
**Должно быть**: `src/features/fill-client-info/ui/` или `src/shared/ui/`
**Файлы**:
- `FormField.tsx` - переиспользуемый компонент → `shared/ui/`
- `FullNameField.tsx` - переиспользуемый компонент → `shared/ui/`
- `IndividualClientForm.tsx` - фича → `features/fill-client-info/ui/`
- `LegalClientForm.tsx` - фича → `features/fill-client-info/ui/`
- `IPClientForm.tsx` - фича → `features/fill-client-info/ui/`
- `GovClientForm.tsx` - фича → `features/fill-client-info/ui/`

#### 1.3. `src/components/FirstStepOfForm/`
**Текущее состояние**: Компонент в `src/components/FirstStepOfForm/`
**Должно быть**: `src/widgets/form-steps/ui/` (уже есть re-export, но файлы дублируются)
**Проблема**: Файлы находятся в `components/`, но используются через `widgets/form-steps`

#### 1.4. `src/components/ForeStepOfInfoObj/`
**Текущее состояние**: Компонент в `src/components/ForeStepOfInfoObj/`
**Должно быть**: `src/features/fill-object-info/ui/` (уже есть re-export, но файлы дублируются)

#### 1.5. `src/components/TwoStepOfAccordion/`
**Текущее состояние**: Компонент в `src/components/TwoStepOfAccordion/`
**Должно быть**: `src/features/select-request-reason/ui/` (уже есть re-export, но файлы дублируются)

#### 1.6. `src/components/SuccessPage/`
**Текущее состояние**: Компонент в `src/components/SuccessPage/`
**Должно быть**: `src/widgets/success-page/ui/` (уже есть re-export, но файлы дублируются)

#### 1.7. `src/components/DocumentsUploadForm.tsx`
**Текущее состояние**: Компонент в `src/components/`
**Должно быть**: `src/features/upload-documents/ui/` (уже есть re-export, но файл дублируется)

#### 1.8. `src/components/ClientInfoStep.tsx`
**Текущее состояние**: Компонент в `src/components/`
**Должно быть**: `src/features/fill-client-info/ui/` (уже есть re-export, но файл дублируется)

#### 1.9. `src/components/ErrorBoundary.tsx`, `LazyErrorBoundary.tsx`, `LoadingFallback.tsx`
**Текущее состояние**: Компоненты в `src/components/`
**Должно быть**: `src/shared/ui/` (уже есть копии в `shared/ui/`, но файлы дублируются)

#### 1.10. `src/components/DialogForm.tsx`
**Текущее состояние**: Компонент в `src/components/`
**Должно быть**: `src/shared/ui/` или `src/widgets/`

#### 1.11. `src/components/lazy/` - пустая директория
**Должно быть**: Удалена

### 2. Директория `src/hooks/` - НЕ соответствует FSD

**Проблема**: Хуки находятся в отдельной директории, должны быть в соответствующих FSD слоях.

**Файлы**:
- `useApiData.ts` - уже есть в `entities/request/model/use-api-data.ts`, но старый файл остался
- `use-mobile.ts` - переиспользуемый хук → `shared/lib/`

**Должно быть**: 
- `src/shared/lib/hooks/` для переиспользуемых хуков
- Или в соответствующих слоях (entities, features)

### 3. Директория `src/lib/` - НЕ соответствует FSD

**Проблема**: Утилиты находятся в `src/lib/`, должны быть в `src/shared/lib/`

**Файлы**:
- `utils.ts` - уже есть в `shared/lib/utils.ts`, но старый файл остался

**Должно быть**: `src/shared/lib/utils.ts` (уже есть, но дублируется)

### 4. Директория `src/api/` - НЕ соответствует FSD

**Проблема**: API находится в `src/api/`, должен быть в `src/entities/*/api/`

**Файлы**:
- `index.ts` - уже есть реэкспорты в `entities/request/api` и `entities/service/api`, но старый файл остался

**Должно быть**: Полностью перемещен в `entities/*/api/`

### 5. Старые директории остались (дублирование)

**Проблема**: Файлы перемещены в FSD слои, но старые директории остались с копиями.

**Директории**:
- `src/config/` - файлы уже в `shared/config/`, но старые остались
- `src/constants/` - файлы уже в `shared/config/`, но старые остались
- `src/types/` - типы реэкспортируются через entities, но директория осталась
- `src/utils/` - файлы уже в `shared/lib/`, но старые остались

### 6. Файлы в корне `src/`

**Проблема**: Некоторые файлы находятся в корне `src/`, должны быть в соответствующих слоях.

**Файлы**:
- `App.css` - должен быть в `app/` или `shared/styles/`
- `polyfills.ts` - должен быть в `shared/lib/` или `app/`
- `main.tsx` - правильно в корне (точка входа)
- `index.css` - правильно в корне (глобальные стили)
- `vite-env.d.ts` - правильно в корне (типы Vite)

## 📊 Статистика нарушений

- ❌ **Компоненты в неправильных местах**: ~70+ файлов
- ❌ **Дублирование файлов**: ~15+ файлов
- ❌ **Хуки в неправильном месте**: 2 файла
- ❌ **Утилиты в неправильном месте**: 1 файл
- ❌ **API в неправильном месте**: 1 файл
- ❌ **Старые директории с копиями**: 4 директории
- ❌ **Пустые директории**: 1 директория

## 🎯 План исправления

### Приоритет 1: Критические нарушения

1. **Переместить `src/components/ui/` → `src/shared/ui/`**
   - Все 60+ UI компонентов
   - Обновить все импорты

2. **Переместить компоненты в соответствующие FSD слои**:
   - `components/forms/` → `features/fill-client-info/ui/` и `shared/ui/`
   - `components/FirstStepOfForm/` → `widgets/form-steps/ui/`
   - `components/ForeStepOfInfoObj/` → `features/fill-object-info/ui/`
   - `components/TwoStepOfAccordion/` → `features/select-request-reason/ui/`
   - `components/SuccessPage/` → `widgets/success-page/ui/`
   - `components/DocumentsUploadForm.tsx` → `features/upload-documents/ui/`
   - `components/ClientInfoStep.tsx` → `features/fill-client-info/ui/`
   - `components/ErrorBoundary.tsx` и др. → `shared/ui/` (удалить дубликаты)

3. **Переместить хуки**:
   - `hooks/use-mobile.ts` → `shared/lib/hooks/`
   - Удалить `hooks/useApiData.ts` (уже есть в entities)

4. **Переместить утилиты**:
   - Удалить `lib/utils.ts` (уже есть в shared/lib)

5. **Переместить API**:
   - Переместить `api/index.ts` в `entities/request/api/` или разделить по entities

### Приоритет 2: Очистка

6. **Удалить старые директории** (после проверки, что все импорты обновлены):
   - `src/config/`
   - `src/constants/`
   - `src/types/` (или переместить типы в entities)
   - `src/utils/`
   - `src/api/` (после перемещения)
   - `src/components/lazy/` (пустая)

7. **Переместить файлы из корня**:
   - `App.css` → `app/App.css` или `shared/styles/`
   - `polyfills.ts` → `shared/lib/polyfills.ts`

## ⚠️ Важные замечания

1. **shadcn/ui**: Компоненты в `components/ui/` могут быть сгенерированы shadcn/ui. Нужно проверить `components.json` и возможно обновить пути генерации.

2. **Импорты**: После перемещения нужно обновить ВСЕ импорты в проекте.

3. **Тестирование**: После каждого перемещения нужно проверять, что проект собирается и работает.

